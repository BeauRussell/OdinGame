{
    "version": "0.2.0",
    "configurations":[
        {
            "name": "Debug",
            "request": "launch",
            "preLaunchTask": "Build",
            "type": "lldb",
            "program": "${workspaceFolder}/${workspaceFolderBasename}",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}